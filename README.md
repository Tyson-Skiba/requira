# Requira

To start run `yarn dev`, note that you need to install `,eta flac` on macs you can do this via brew

```shell
brew install flac
```

## Schema Change

`npx prisma migrate dev --name name`

then

`npx prisma generate`
