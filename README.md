# Requira

To start run `yarn dev`, note that you need to install `metaflac` on macs you can do this via brew

```shell
brew install flac
```

## Running

To run the app in production mode refer to the example docker compose

## Schema Change

`npx prisma migrate dev --name name`

then

`npx prisma generate`
